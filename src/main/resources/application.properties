spring.datasource.url=jdbc:mysql://localhost:3306/test
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.jdbc.Driver


  ###################以下为druid增加的配置###########################
#druid数据源配置
spring.datasource.type=com.alibaba.druid.pool.DruidDataSource
    # 下面为连接池的补充设置，应用到上面所有数据源中
    # 初始化大小，最小，最大
    spring.datasource.initialSize= 5
    spring.datasource.minIdle= 5
    spring.datasource.maxActive= 20
    # 配置获取连接等待超时的时间
    spring.datasource.maxWait=60000
    # 配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒
    spring.datasource.timeBetweenEvictionRunsMillis=60000
    # 配置一个连接在池中最小生存的时间，单位是毫秒
    spring.datasource.minEvictableIdleTimeMillis=300000
    spring.datasource.validationQuery=SELECT 1 FROM DUAL
    spring.datasource.testWhileIdle=true
    spring.datasource.testOnBorrow=false
    spring.datasource.testOnReturn=false
    # 打开PSCache，并且指定每个连接上PSCache的大小
    spring.datasource.poolPreparedStatements=true
    spring.datasource.maxPoolPreparedStatementPerConnectionSize=20
    # 配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    spring.datasource.filters=stat,wall,log4j
    # 通过connectProperties属性来打开mergeSql功能；慢SQL记录
    spring.datasource.connectionProperties=druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
    # 合并多个DruidDataSource的监控数据
     spring.datasource.useGlobalDataSourceStat=true
    ###############以上为配置druid添加的配置########################################





#配置访问路径
#server.context-path=/myproject
#server.port=8082
management.port=8081
spring.mvc.view.prefix=/WEB-INFO/
spring.mvc.view.subfix=.jsp

#test11=1111
user.username: ssq${random.int(10)}
user.age: 18
user.class-grade.yuwen: 67
user.class-grade.shuxue:99


#可以配置的属性都是来源于这个配置的properties类
spring.http.encoding.charset=utf-8
spring.http.encoding.enabled=true
#强制编码
spring.http.encoding.force=true

#日志  Java包下面所有日志级别为info
logging.level.com.ssq.www=info
#设置日志文件输出位置  当前磁盘根目录下的log/mylogger.log
logging.file=/log/mylogger.log
# 设置日志格式   logging.pattern.console
#单个日志大小
#logging.file.max-size=20

#配置静态资源文件夹
#spring.resources.static-locations=classpath:/static



#整合mybatis  使用配置文件的方式这个配置不可少
mybatis.mapper-locations=classpath:com/ssq/www/mapper/*.xml
#需要别的配置属性开启这个
#mybatis.config-locations=classpath:mybatis/config.xml
